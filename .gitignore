# Dependencies
node_modules/

# Local env (never commit real secrets — .env.example is tracked)
.env
.env.*
!.env.example

# Runtime output
uploads/
logs/
*.log

# Build
dist/

# OS
.DS_Store

# NOTE: prisma/migrations/ IS committed — it is required for `prisma migrate deploy`.
